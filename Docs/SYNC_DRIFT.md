# Sync Drift Report

> Auto-generated by cider-sync-validator on 2026-03-16

## Status: 1 discrepancy found

### Desktop: Missing `enrichmentStatus` in push payload

**Spec says** (SYNC_PROTOCOL.md, "Sync Behavior by Platform" table):
> Desktop: Runs enrichment pipeline locally, pushes `"complete"`

**Actual behavior**: `enrichmentStatus` is completely absent from the Desktop push payload in `SyncService.swift`. Desktop pushes `aiSummary` and `dominantColors` after local enrichment, but never sets `enrichmentStatus` to `"complete"`.

**Impact**: The server (and other clients pulling) won't know a bookmark's enrichment is finished based on the status field alone. The server-side enrichment action may re-run enrichment on bookmarks that Desktop already enriched, since `enrichmentStatus` stays `null` or `"pending"` on the server.

**Fix**: In `SyncService.swift`, add `enrichmentStatus` to the bookmark push payload dictionary. Set it to `"complete"` when `aiSummary` is present, or `"pending"` otherwise.

**Approximate location**: `SyncService.swift`, bookmark push payload builder (around lines 833-851).

---

## Passing checks

| Platform | Check | Result |
|----------|-------|--------|
| Desktop | Push payload fields match spec (except above) | PASS |
| Desktop | `ciderSyncId` lowercased | PASS |
| Desktop | Soft-delete pattern (deleted + deletedAt) | PASS |
| Desktop | No hard deletes | PASS |
| iOS | `PushBookmark` fields match spec | PASS |
| iOS | `PushFolder` fields match spec | PASS |
| iOS | `ciderSyncId` lowercased | PASS |
| iOS | `enrichmentStatus` set to `"pending"` | PASS |
| iOS | Soft-delete pattern | PASS |
| Web | Schema matches DATA_MODEL.md (all tables) | PASS |
| Web | Strict `v.object()` push validation | PASS |
| Web | Last-write-wins on `updatedAt` | PASS |
| Web | Equal-timestamp rule (server canonical for folders) | PASS |
| Web | Sync endpoints match SYNC_PROTOCOL.md | PASS |
