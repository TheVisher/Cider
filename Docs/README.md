# Cider Docs

Status: pointer to canonical docs.

Start with `Docs/INDEX.md`.

`Docs/INDEX.md` is the canonical docs entrypoint and active core-doc list. It also owns the durable policy for what belongs in docs, what belongs on Kanban, and how to treat legacy Markdown.
